export type User = { id: string; username: string; role: "admin" | "user" | "readonly"; permissions: string[]; must_change_password: boolean };
export type Profile = { id: string; name: string; shape: "SQUARE" | "ROUND" | "RECTANGULAR"; material: string; compatibility_group: string; width_mm?: number; height_mm?: number; outside_diameter_mm?: number; nominal_size?: string; wall_thickness_mm?: number; supplier_id?: string; supplier_name?: string; purchase_price_per_meter_cents: number; sale_price_per_meter_cents: number; weight_per_meter_kg: number; image_path?: string; notes?: string; catalog_source?: string; raw_length_mm: number };
export type Connector = { product_id: string; sku: string; name_tr: string; connector_role?: string; compatibility_group?: string; profile_shape?: string; compatibility_status: "COMPATIBLE" | "UNRESOLVED"; compatibility_source?: "STRUCTURED" | "SKU_FALLBACK" | "MANUAL"; compatibility_note?: string; sale_price_cents: number; purchase_cost_cents: number; central_stock: number; image?: string; material?: string; size?: string; form?: string; form_code?: string; model?: string; normalized_material?: string; normalized_size?: string; normalized_tube_type?: string; normalized_pipe_size?: string };
export type Complementary = { id: string; name: string; sku_optional?: string; description?: string; unit_type: "PIECE" | "METER" | "M2"; purchase_unit_price_cents: number; sale_unit_price_cents: number; supplier_id?: string; image_path?: string; website_url_optional?: string; notes?: string; catalog_source?: string };
export type SyncStats = { lastSyncedAt: string | null; lastAttemptAt: string | null; panelReachable: boolean; lastError: string | null; connectorCount: number; compatibleCount: number; unresolvedCount: number; profileCount: number; complementCount: number };
export type Supplier = { id: string; name: string };
export type Bootstrap = { user: User; profiles: Profile[]; connectors: Connector[]; complementaryProducts: Complementary[]; suppliers: Supplier[]; settings: Record<string, string>; sync: SyncStats };
export type KitSummary = { product_cost_cents: number; extra_cost_cents: number; total_cost_cents: number; sale_price_cents: number; net_revenue_cents?: number; output_vat_cents?: number; gross_profit_cents?: number; profit_cents: number; net_profit_cents?: number; margin_percent: number };
export type KitImage = { id: string; image_path: string; sort_order: number };
export type KitVariant = { id: string; name: string; profile_id: string; profile_name?: string; status: string; connectors: any[]; cuts: any[]; complementary_items: any[]; configuration?: { material: string; shape: string; compatibility_group: string; wall_thickness_mm: number; size: string }; pricing?: any; summary: KitSummary };
export type Kit = { id: string; name: string; sku?: string | null; description?: string; status: string; sale_price_cents: number; labor_cost_cents: number; packaging_cost_cents: number; other_cost_cents: number; updated_at: string; thumbnail?: string | null; summary: KitSummary; variants: KitVariant[]; images?: KitImage[] };
