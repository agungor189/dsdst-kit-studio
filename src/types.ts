export type User = { id: string; username: string; role: "admin" | "user" | "readonly"; permissions: Record<string, boolean>; must_change_password: boolean };
export type EconomicsStatus = "UNKNOWN" | "KNOWN";
export type BaseUomCode = "piece" | "meter" | "square_meter" | "kg" | "roll" | "package" | "box";
export type Profile = { id: string; name: string; shape: "SQUARE" | "ROUND" | "RECTANGULAR"; material: string; compatibility_group: string; width_mm?: number; height_mm?: number; outside_diameter_mm?: number; nominal_size?: string; wall_thickness_mm?: number; supplier_id?: string; supplier_name?: string; purchase_price_per_meter_cents: number; sale_price_per_meter_cents: number; cost_status: EconomicsStatus; sale_price_status: EconomicsStatus; markup_basis_points: number; weight_per_meter_kg: number; image_path?: string; notes?: string; catalog_source?: string; catalog_version_ref?: string; base_uom_code?: BaseUomCode; raw_length_mm: number };
export type Connector = { product_id: string; sku: string; name_tr: string; connector_role?: string; compatibility_group?: string; profile_shape?: string; compatible_material_group?: string; profile_width_mm?: number; profile_height_mm?: number; outside_diameter_mm?: number; nominal_size?: string; wall_min_mm?: number; wall_max_mm?: number; compatibility_status: "COMPATIBLE" | "UNRESOLVED"; compatibility_source?: "STRUCTURED" | "SKU_FALLBACK" | "MANUAL"; compatibility_note?: string; sale_price_cents: number; purchase_cost_cents: number; cost_status: EconomicsStatus; sale_price_status: EconomicsStatus; unit_weight_grams?: number | null; central_stock: number; image?: string; material?: string; size?: string; form?: string; form_code?: string; model?: string; normalized_material?: string; normalized_size?: string; normalized_tube_type?: string; normalized_pipe_size?: string; catalog_version_ref?: string; base_uom_code?: BaseUomCode };
export type Complementary = { id: string; name: string; sku_optional?: string; description?: string; unit_type: "PIECE" | "METER" | "M2"; base_uom_code: BaseUomCode; purchase_unit_price_cents: number; sale_unit_price_cents: number; cost_status: EconomicsStatus; sale_price_status: EconomicsStatus; markup_basis_points: number; weight_per_unit_grams?: number | null; supplier_id?: string; image_path?: string; website_url_optional?: string; notes?: string; catalog_source?: string; catalog_version_ref?: string };
export type SyncStats = { lastSyncedAt: string | null; lastAttemptAt: string | null; panelReachable: boolean; lastError: string | null; connectorCount: number; compatibleCount: number; unresolvedCount: number; profileCount: number; complementCount: number };
export type Supplier = { id: string; name: string };
export type Bootstrap = { user: User; profiles: Profile[]; connectors: Connector[]; complementaryProducts: Complementary[]; suppliers: Supplier[]; settings: Record<string, string>; sync: SyncStats };
export type KitSummary = { product_cost_cents: number; extra_cost_cents: number; total_cost_cents: number; sale_price_cents: number; subtotal_ex_vat_cents?: number; vat_cents?: number; profit_cents: number; margin_percent: number; total_weight_grams?: number; weight_complete?: boolean };
export type PricingResult = { connector_cost_cents: number; profile_cost_cents: number; complementary_cost_cents: number; component_cost_cents: number; labor_cost_cents: number; packaging_cost_cents: number; other_cost_cents: number; extra_cost_cents: number; total_cost_cents: number; connector_sale_cents: number; profile_sale_cents: number; complementary_sale_cents: number; connector_profit_cents: number; profile_profit_cents: number; complementary_profit_cents: number; subtotal_ex_vat_cents: number; profit_cents: number; margin_basis_points: number; margin_percent: number; vat_rate_basis_points: number; vat_cents: number; total_inc_vat_cents: number; connector_weight_grams: number; profile_weight_grams: number; complementary_weight_grams: number; total_weight_grams: number; weight_complete: boolean; missing_weight_items: Array<{ type: string; id?: string; name: string; quantity: number }>; connectors: any; profiles: any; complementary: any };
export type KitImage = { id: string; image_path: string; sort_order: number };
export type KitVariant = { id: string; name: string; profile_id: string | null; profile_name?: string; status: string; connectors: any[]; cuts: any[]; complementary_items: any[]; configuration?: { material: string; shape: string; compatibility_group: string; wall_thickness_mm: number; size: string }; pricing?: any; summary: KitSummary };
export type Kit = { id: string; name: string; sku?: string | null; description?: string; status: string; sale_price_cents: number; labor_cost_cents: number; packaging_cost_cents: number; other_cost_cents: number; derived_from_kit_id?: string | null; updated_at: string; thumbnail?: string | null; summary: KitSummary; variants: KitVariant[]; images?: KitImage[] };
export type ConversionPreview = { status: "FULL" | "PARTIAL" | "INCOMPATIBLE"; missing_roles: string[]; source: { id: string; profile_id: string; configuration: KitVariant["configuration"]; pricing: any; summary: KitSummary }; target: { profile: Profile; connectors: Array<{ role: string; quantity: number; product_id: string | null; sku: string | null }>; pricing: any; summary: KitSummary & { total_weight_grams: number } } };
